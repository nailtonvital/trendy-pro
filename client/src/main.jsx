import React from 'react'
import ReactDOM from 'react-dom/client'
import RoutingApp from './routes/index'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RoutingApp />
  </React.StrictMode>
)
